# HTTP Ingress Example

This example exposes the app publicly and allows insecure HTTP traffic (`ingress_allow_insecure_connections = true`).

Use this only for local/testing scenarios.

## Run

```bash
terraform init
terraform plan
```
