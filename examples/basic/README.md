# Basic Example

This example shows how to consume the root module from this repository.

## Run

```bash
terraform init
terraform plan
```

You must authenticate to Azure before running this example.
